import { ImageNameTrimmerPipe } from './image-name-trimmer.pipe';

describe('ImageNameTrimmerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageNameTrimmerPipe();
    expect(pipe).toBeTruthy();
  });
});
